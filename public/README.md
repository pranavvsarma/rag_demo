# public

Static assets served at the site root by Next.js. Default Next.js starter icons, unused beyond scaffolding unless referenced from a component.

- `file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg` — default Next.js icon assets.
