# .claude

Claude Code tool configuration for this project.

- `launch.json` — dev-server launch config (`npm run dev` on port 3000) so Claude Code can start/attach to the app.
- `settings.local.json` — local, machine-specific permission grants for Claude Code (not shared via version control conventions elsewhere, but committed here for this project). Do not put secrets here.
