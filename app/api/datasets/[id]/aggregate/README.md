# app/api/datasets/[id]/aggregate

`route.ts` — groups and aggregates the real rows of a dataset in Node (server-side), for chart/report building in the Data Explorer. Numbers are always computed from actual data, never produced by the LLM.
