# app/hooks

- `useChat.ts` — conversation state, streamed-response consumption (parses the `{sources}` framing line, then token-by-token text), and `attachDataset` / `openCatalogDataset` / `openReport` deep-link handling.
